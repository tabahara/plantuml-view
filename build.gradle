apply plugin: 'java'

task wrapper(type:Wrapper){ 
  gradleVersion="2.2"
}

repositories { 
  flatDir(dirs: file('/usr/share/java'))
}

dependencies { 
  compile 'batik:batik-all:1.7'
}

jar { 
  manifest { 
    attributes("Main-Class" : "com.quinmantha.tool.Main");
  }
}